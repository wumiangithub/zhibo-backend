# 后端进度

架构/需求/API 以 `../zhibo-shared/docs/` 为准，本文件只记后端勾选。

## 当前状态

- **下一步**：第四迭代已验收；无排期任务。
- **阻塞**：无。

## 已核实的微吼事实

- 域名：`https://saas-open.vhall.com`
- `POST` + `application/x-www-form-urlencoded` + UTF-8；部分接口为 **GET + query**（如 get-role-url、get-token 拼嵌入）
- 头：`platform: 15`，`request-id` 每次唯一
- 公共参数：`app_key`、`sign_type`(0=MD5)、`signed_at`、`sign`
- 签名：除 `sign` 外参数按名升序拼 `名+值`，首尾加 secret，MD5 小写；数组只签 key。文档：[签名机制](https://saas-doc.vhall.com/opendocs/show/1423)
- 成功：`code=200`（我们对外 API 仍用 `code=0`）

| 功能 | 路径 | 要点 |
|------|------|------|
| 创建活动 | `POST /v3/webinars/webinar/create` | subject、start_time、webinar_type 必填；`start_time` 为 `Y-m-d H:i`（不要秒） |
| 活动列表 | `POST /v3/webinars/webinar/get-list` | pos、limit(≤100) 必填 |
| 活动详情 | `POST /v3/webinars/webinar/info` | webinar_id；`full_embed_share_link`、`pv` |
| 改活动 | `POST /v3/webinars/webinar/edit` | webinar_id + subject / start_time（到分钟）；→ `{webinar_id}` |
| 结束直播 | `POST /v3/webinars/live/end` | webinar_id；结束后 `webinar_state` 多为 3 |
| 删除活动 | `POST /v3/webinars/webinar/delete` | **`webinar_ids`**（复数，可传单个 id 字符串） |
| 主持人开播 | `GET /v3/webinars/live/get-role-url` | webinar_id + type=1 → `page_url` |
| 控制台 token | `POST /v3/users/open-user/get-token` | 拼管理后台嵌入 URL |
| 当前在线 | `POST /v3/data-center/webinar/current-online-number` | → `num`；约 1 次/分钟 |
| 在线趋势 | `POST /v3/data-center/report/online` | start/end 跨度 ≤1 天 |

`webinar_state`：`0全部 1直播 2预告 3结束 4点播 5回放`

### 已落地对外 API

| 服务 | 方法 | 路径 | 说明 |
|------|------|------|------|
| admin | POST | `/api/activities` | body: `title`,`startTime`,`type` → `{id}` |
| admin | GET | `/api/activities` | query: `page`,`pageSize`,`keyword`,`state` |
| admin | GET | `/api/activities/{id}` | 详情 |
| admin | GET | `/api/activities/{id}/host` | → `{hostUrl}` |
| admin | GET | `/api/activities/{id}/embed` | → `{embedUrl}`（控制台 token） |
| admin | GET | `/api/activities/{id}/stats` | → `{id,title,state,type,pv,onlineCount}` |
| admin | GET | `/api/activities/{id}/stats/online` | query: `startTime`,`endTime` → 分钟级在线点 |
| admin | PUT | `/api/activities/{id}` | body: `title?`,`startTime?` → `{id}` |
| admin | POST | `/api/activities/{id}/end` | → `{id,state}` |
| admin | DELETE | `/api/activities/{id}` | → `{id}` |
| live | GET | `/api/watch/activities` | 精简列表：id/title/state/startTime/coverUrl |
| live | GET | `/api/watch/{id}` | query: `guestId?`,`nickname?` → `embedUrl` + `startTime`/`endTime` |
| live | GET | `/api/watch/{id}/sdk` | query: `guestId?`,`nickname?` → SDK 载荷 + `embedUrl` + `startTime`/`endTime` |

## 任务

- [x] T0.1 Maven 多模块骨架
- [x] T0.2 Maven Wrapper
- [x] T1.1 `VhallProperties` + AutoConfiguration
- [x] T1.2 `VhallSignUtil`
- [x] T1.3 `VhallResponse` + `VhallException`
- [x] T1.4 `VhallClient`（含 GET）
- [x] T1.5 两服务 `ApiResponse` + 全局异常
- [x] T1.6 Mock 测试
- [x] T2 活动 API
- [x] T3.1 观看 `GET /api/watch/{id}`
- [x] T3.2 开播 `GET /api/activities/{id}/host`
- [x] T3.3 管理后台嵌入
- [x] T4 统计 API
- [x] T7 联调（真开播画面已验收）
- [x] T8.1 统计接口加固（跨度 / 时间裁分钟 / 详情 endTime）
- [x] T8.2 当前在线 60s 短缓存
- [x] T9.0 核实微吼改/结束/删（edit / live/end / delete+webinar_ids）
- [x] T9.1 `PUT /api/activities/{id}`
- [x] T9.2 `POST /api/activities/{id}/end`
- [x] T9.3 `DELETE /api/activities/{id}`
- [x] T9.4 live `GET /api/watch/activities`
- [x] T10.1 SDK 初始化载荷 `GET /api/watch/{id}/sdk`（6 字段签名，signedat 无下划线，无 sign_type；含 embedUrl 兜底；Mock 单测含签名向量对照）
- [x] T10.7 观看态 `startTime`/`endTime`（`/sdk` + `/watch/{id}`；`end_time` 以 `0000` 开头不输出；Mock 单测）

## 变更日志

- 2026-09-20：第四迭代验收——T10.1 / T10.7 已交付；无后续排期。
- 2026-09-18：T10.7——`GET /api/watch/{id}/sdk` 与 `GET /api/watch/{id}` 响应增加 `startTime`/`endTime`（来自微吼 info 的 `start_time`/`end_time`）；`end_time` 以 `0000` 开头（未结束占位）时不设值，jackson `non_null` 不输出。过滤逻辑抽到 `WatchIdentity.resolveEndTime`。Mock：占位省略 / 真实 endTime 映射。
- 2026-09-18：T10.1 SDK 初始化载荷——`GET /api/watch/{id}/sdk`：拉微吼 `info` 取 `full_embed_share_link` 拼 `embedUrl` 兜底；按微吼 JS SDK 规则签名（仅 6 字段 `account`/`email`/`username`/`roomid`/`app_key`/`signedat`，`signedat` 无下划线，**不加 `sign_type`**），复用 `VhallSignUtil`；secret 不出后端。身份/嵌入逻辑抽到 `WatchIdentity` 供 watch 与 sdk 共用。新增 `TimeConfig`(Clock bean) 供 SDK 签名注入固定时钟测。Mock 单测 7 项含签名向量对照。
- 2026-09-18：T9.0～T9.4——联调核实 `edit` / `live/end` / `delete(webinar_ids)`；管理端改/结束/删 + 观看端精简列表；T9.7 总控验收通过。
- 2026-09-18：T8.1 / T8.2——趋势跨度 >1 天业务 400；发给微吼裁到分钟；详情 `0000…` endTime 不输出；同活动当前在线缓存 60s（可注入 `Clock`）。
- 2026-09-18：第一迭代验收——台式机创建/观看、笔记本开播能看到画面。
- 2026-09-17：T7 联调——`.env` 加载太晚会绑空密钥（已把 PropertySource 插到 yml 之上）；微吼 create 的 `start_time` 只要 `Y-m-d H:i`。嵌入页已通。
- 2026-09-17：T4 统计——概览（info.pv + 当前在线）与时间段在线趋势；契约草稿记在本表，待文档 agent 写入 API约定。
- 2026-09-17：T3.3 管理后台嵌入——`get-token` + `https://e.vhall.com/v3/embed/live/detail/{id}?token=`。
- 2026-09-17：T3.1/T3.2——观看拼 `email`/`nickname` 到 `full_embed_share_link`；开播走 GET `get-role-url`；`VhallClient` 补 GET。
- 2026-09-17：T2 活动 API（创建/列表/详情）；详情微吼路径核实为 `/v3/webinars/webinar/info`；对外字段用 activity 语义。
- 2026-09-17：T1.6 Mock 收尾——签名 / 响应 / Client（含 HTTP 失败）/ 两服务异常映射全绿；业务 API Mock 随 T2+ 补。
- 2026-09-17：T1.5 两服务各自 `ApiResponse` + `GlobalExceptionHandler`（HTTP 200 + 业务 code；微吼异常映射；故意不进 vhall-common）。
- 2026-09-17：T1.4 `VhallClient`（RestClient + 自动签名 + MockRestServiceServer 单测）；AutoConfiguration 注册 Bean。
- 2026-09-17：T1.3 `VhallResponse` + `VhallException`（微吼 `code=200`；`requireData()` 失败抛异常）。对外 `ApiResponse(code=0)` 仍留给 T1.5。
- 2026-09-17：T1.2 `VhallSignUtil`（MD5）；文档正确页是 [签名机制 show/1423](https://saas-doc.vhall.com/opendocs/show/1423)（旧链 1296 是基础概念）。官方样例单测通过。
- 2026-09-17：T0.2 补齐 `mvnw` / `mvnw.cmd`（only-script，Maven 3.9.11）；本机 JDK 17 已可用。
- 2026-09-17：迁到 `zhibo/zhibo-backend`；架构文档改由 `zhibo-shared` 维护。
- 2026-09-17：T1.1 配置类。知识点：AutoConfiguration ≈ 装了就生效的插件。
- 2026-09-16：T0.1 多模块；核实创建活动/列表接口。
