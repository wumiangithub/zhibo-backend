<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.zhibo</groupId>
    <artifactId>zhibo-backend</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>zhibo-backend</name>
    <description>直播项目后端（多模块）- 对接微吼(Vhall)开放平台</description>

    <modules>
        <module>vhall-common</module>
        <module>admin-service</module>
        <module>live-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!--
      dependencyManagement ≈ pnpm catalog：只锁定版本，不会自动装进子模块。
      子模块要自己在 <dependencies> 里声明才会真正引入。
      不要把 lombok/test 写在本文件的 <dependencies> 里，否则三个模块会被强加。
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.zhibo</groupId>
                <artifactId>vhall-common</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
